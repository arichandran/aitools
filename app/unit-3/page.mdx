import PyodideRunner from "@/components/PyodideRunner";
import CodeReference from "@/components/CodeReference";
import PromptSandbox from "@/components/PromptSandbox";

# <a id="_Toc239677981"></a>UNIT 3: AI Tools for Data Analysis and Visualization

## <a id="_Toc239677982"></a>1\. Detailed Unit Content

### <a id="_Toc239677983"></a>3\.1 AI\-Assisted Spreadsheet and Dataset Analysis Using Natural Language

Traditionally, analysing a dataset required knowing spreadsheet formulas \(VLOOKUP, PIVOT TABLE\) or a programming language \(Python/pandas, R\)\. AI\-assisted data\-analysis tools \(ChatGPT's Advanced Data Analysis / Code Interpreter, Microsoft Copilot in Excel, Julius AI\) remove this barrier: a user uploads a file \(\.csv, \.xlsx\) and asks questions in plain English, such as 'What is the average marks per section?' The tool internally writes and runs code \(typically Python\) to compute the answer, then explains the result in natural language — combining the rigor of programmatic computation with the accessibility of conversational interaction\.

### <a id="_Toc239677984"></a>3\.2 Natural\-Language Querying of Datasets

A natural\-language query is a plain\-English question about the data rather than a formula\. Effective queries are specific about the column names, the aggregation required \(sum, average, count, maximum\), and any filter conditions\. Compare a vague query \('tell me about this data'\) with a precise one \('What is the average attendance percentage for students in Section B, and how many students in that section have attendance below 75%?'\) — the precise query yields a directly usable, checkable answer\.

### <a id="_Toc239677985"></a>3\.3 AI Tools for Generating Charts and Visual Summaries

Once a dataset is uploaded, most AI data-analysis tools can generate a chart on request — e.g., 'plot a bar chart of average marks by subject' or 'show a pie chart of pass vs fail counts'. Choosing the right chart type matters for correct interpretation:


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Chart Type</th>
      <th>Best Used For</th>
      <th>Avoid When</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><strong>Bar chart</strong></td>
      <td>Comparing a numeric value across discrete categories (e.g., average marks per subject).</td>
      <td>Showing change over continuous time (use a line chart instead).</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><strong>Line chart</strong></td>
      <td>Showing a trend over an ordered sequence, typically time (e.g., attendance over weeks).</td>
      <td>Comparing unrelated categories with no natural order.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td><strong>Pie chart</strong></td>
      <td>Showing proportion of a whole, with very few categories (2–5).</td>
      <td>More than 5–6 categories, or when precise comparison between two similar-sized slices matters (bar chart reads more accurately).</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>4</td>
      <td><strong>Scatter plot</strong></td>
      <td>Showing the relationship/correlation between two numeric variables (e.g., study hours vs marks).</td>
      <td>Categorical data with no continuous numeric pairing.</td>
    </tr>
  </tbody>
</table>

<div style={{ margin: '2rem 0', textAlign: 'center' }}>
  <img src="/images/bar_chart_33.png" alt="Bar Chart: Average Marks by Subject" style={{ maxWidth: '450px', borderRadius: '8px', border: '1px solid #ccc' }} />
  <p style={{ fontSize: '0.9rem', color: '#666', marginTop: '0.5rem' }}><em>Example: Comparing average marks across discrete subjects.</em></p>
</div>

<div style={{ margin: '2rem 0', textAlign: 'center' }}>
  <img src="/images/line_chart_33.png" alt="Line Chart: Attendance over Weeks" style={{ maxWidth: '450px', borderRadius: '8px', border: '1px solid #ccc' }} />
  <p style={{ fontSize: '0.9rem', color: '#666', marginTop: '0.5rem' }}><em>Example: Showing the continuous trend of class attendance over 8 weeks.</em></p>
</div>

<div style={{ margin: '2rem 0', textAlign: 'center' }}>
  <img src="/images/pie_chart_33.png" alt="Pie Chart: Final Exam Results" style={{ maxWidth: '350px', borderRadius: '8px', border: '1px solid #ccc' }} />
  <p style={{ fontSize: '0.9rem', color: '#666', marginTop: '0.5rem' }}><em>Example: Showing the proportion of different final exam results.</em></p>
</div>

<div style={{ margin: '2rem 0', textAlign: 'center' }}>
  <img src="/images/scatter_plot_33.png" alt="Scatter Plot: Study Hours vs Marks" style={{ maxWidth: '450px', borderRadius: '8px', border: '1px solid #ccc' }} />
  <p style={{ fontSize: '0.9rem', color: '#666', marginTop: '0.5rem' }}><em>Example: Demonstrating the positive correlation between study hours and final marks.</em></p>
</div>

### <a id="_Toc239677986"></a>3\.4 Interpreting AI\-Generated Statistical Summaries Correctly

An AI tool can compute mean, median, mode, standard deviation, and correlation instantly, but interpreting what these numbers mean is the student's responsibility. Some recurring pitfalls:


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Statistical Metric / Concept</th>
      <th>Common Interpretation Pitfall</th>
      <th>Correct Student Analysis</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><strong>Mean vs Median</strong></td>
      <td>Extreme outliers pull the mean away from the typical value; AI often reports only the mean without distribution checks.</td>
      <td>Use the median for skewed distributions (e.g., salaries, attendance) because it resists extreme outliers.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><strong>Correlation vs Causation</strong></td>
      <td>AI reports a statistical correlation and implies that one variable caused changes in the other.</td>
      <td>Check for lurking or confounding variables; a correlation merely indicates mutual mathematical association.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td><strong>Sample Size (N)</strong></td>
      <td>AI presents metrics with equal confidence regardless of whether $N=5$ or $N=5000$.</td>
      <td>Always evaluate statistical power; tiny samples carry large standard errors and high randomness.</td>
    </tr>
  </tbody>
</table>

### <a id="_Toc239677987"></a>3\.5 Identifying and Correcting Errors in AI\-Generated Data Analysis

AI data\-analysis tools make a specific, well\-documented category of mistake: misinterpreting a column's data type\. For example, a 'Roll Number' or 'Pincode' column contains digits but is categorical/identifier data, not something that should be averaged or summed\. Similarly, a date column stored as text \(e\.g\., '01\-02\-2026'\) may be misread as day\-first or month\-first depending on locale assumptions\. Detecting and correcting such errors requires the student to inspect a few raw rows of the actual dataset and sanity\-check any AI\-reported statistic against them\.

__Rule of thumb:__

Never accept a statistic on a column without first confirming what that column represents\. If an AI tool reports the 'average' of a Roll Number column, that is an immediate red flag that it has treated an identifier as a quantity\.

### <a id="_Toc239677988"></a>2\. List of Experiments

### <a id="_Toc239677989"></a>Experiment 1: Summary Statistics Report and Chart from a Small Dataset

Task: Upload a small dataset (e.g., class attendance or survey data) to an AI tool and generate a summary-statistics report and chart using natural-language commands.

<details>

<summary><b>Worked Solution — Attendance Dataset & Statistical Summary</b></summary>

**Sample Dataset (Class Attendance, 10 Students):**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Student ID</th>
      <th>Section</th>
      <th>Attendance (%)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td>S1</td>
      <td>A</td>
      <td>92</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td>S2</td>
      <td>A</td>
      <td>78</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td>S3</td>
      <td>A</td>
      <td>65</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>4</td>
      <td>S4</td>
      <td>A</td>
      <td>88</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>5</td>
      <td>S5</td>
      <td>A</td>
      <td>95</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>6</td>
      <td>S6</td>
      <td>B</td>
      <td>70</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>7</td>
      <td>S7</td>
      <td>B</td>
      <td>82</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>8</td>
      <td>S8</td>
      <td>B</td>
      <td>60</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>9</td>
      <td>S9</td>
      <td>B</td>
      <td>91</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>10</td>
      <td>S10</td>
      <td>B</td>
      <td>74</td>
    </tr>
  </tbody>
</table>

**Natural-language command given to the AI tool:**
> "Using this attendance dataset, report the mean, median, and minimum attendance for each section, and identify any student below 75% attendance. Then create a bar chart of average attendance by section."

**Summary Statistics Output Report:**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Section</th>
      <th>Student Count</th>
      <th>Mean Attendance (%)</th>
      <th>Median Attendance (%)</th>
      <th>Minimum Attendance (%)</th>
      <th>Students Below 75%</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><strong>Section A</strong></td>
      <td>5</td>
      <td>83.6%</td>
      <td>88.0%</td>
      <td>65.0%</td>
      <td>S3 (65%)</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><strong>Section B</strong></td>
      <td>5</td>
      <td>75.4%</td>
      <td>74.0%</td>
      <td>60.0%</td>
      <td>S6 (70%), S8 (60%), S10 (74%)</td>
    </tr>
  </tbody>
</table>

*Manual Verification Check:*
- **Section A:** $(92 + 78 + 65 + 88 + 95) / 5 = 418 / 5 = 83.6\%$. Sorted = [65, 78, 88, 92, 95] → Median = 88.0%. Min = 65%.
- **Section B:** $(70 + 82 + 60 + 91 + 74) / 5 = 377 / 5 = 75.4\%$. Sorted = [60, 70, 74, 82, 91] → Median = 74.0%. Min = 60%.
- **Below 75% Threshold:** S3 (65%, A), S6 (70%, B), S8 (60%, B), S10 (74%, B).

</details>

__*Key Concepts Applied*__

- Natural-language querying (Section 3.2) with a compound instruction: statistics + a filter condition + a chart request, all in one prompt.
- Choosing a bar chart (Section 3.3) is correct here because we are comparing a numeric average across two discrete categories (Section A vs B), not a trend over time.

<details>

<summary><b>Expected Output</b></summary>

A short table (Mean, Median, Minimum per section, matching the manual calculation above), a list of the four below-75% students, and a two-bar chart showing Section A's average (83.6%) taller than Section B's average (75.4%).

</details>

<br/>
<PromptSandbox initialPrompt="Using this attendance dataset, report the mean, median, and minimum attendance for each section, and identify any student below 75% attendance:

Section A: S1=92%, S2=78%, S3=65%, S4=88%, S5=95%
Section B: S6=70%, S7=82%, S8=60%, S9=91%, S10=74%" />

<br/>
<CodeReference code={`import statistics

section_a = [92, 78, 65, 88, 95]
section_b = [70, 82, 60, 91, 74]

for name, data in [("Section A", section_a), ("Section B", section_b)]:
    print(f"{name}:")
    print(f"  Mean   = {statistics.mean(data):.1f}%")
    print(f"  Median = {statistics.median(data):.1f}%")
    print(f"  Min    = {min(data)}%")

print("\nStudents below 75% attendance:")
all_students = {"S1":92,"S2":78,"S3":65,"S4":88,"S5":95,"S6":70,"S7":82,"S8":60,"S9":91,"S10":74}
for s, v in all_students.items():
    if v < 75:
        print(f"  {s}: {v}%")`} />
<PyodideRunner initialCode={`# Type the code below this line to practice\n`} />

### <a id="_Toc239677990"></a>Experiment 2: Detecting a Misinterpreted Column \(Numeric Code Treated as a Number\)

Task: Show an example where an AI tool misinterprets data (e.g., treats a numeric code as text, or vice-versa), and demonstrate how to verify and correct it.

<details>

<summary><b>Worked Solution</b></summary>

**Sample Student Dataset:**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Student ID</th>
      <th>Roll_No (Identifier)</th>
      <th>Marks (Quantitative)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td>S1</td>
      <td>21A1001</td>
      <td>78</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td>S2</td>
      <td>21A1002</td>
      <td>85</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td>S3</td>
      <td>21A1003</td>
      <td>91</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>4</td>
      <td>S4</td>
      <td>21A1004</td>
      <td>67</td>
    </tr>
  </tbody>
</table>

Suppose an instructor mistakenly asks: "What is the average Roll_No?" A poorly guarded AI data tool might attempt to average the numeric portions and report a nonsensical figure (e.g., 'average Roll_No ≈ 21A1002.5'), because it is treating an identifier field as a quantity.

**Column Diagnostics and Pitfall Analysis:**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Column Name</th>
      <th>Apparent Format</th>
      <th>Actual Data Type</th>
      <th>Permissible Operations</th>
      <th>Invalid Operation (AI Pitfall)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><code>Student</code></td>
      <td>Text</td>
      <td>Identifier / Categorical</td>
      <td>Grouping, counts</td>
      <td>Mathematical averaging</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><code>Roll_No</code></td>
      <td>Alphanumeric (looks numeric)</td>
      <td>Unique Key / Identifier</td>
      <td>Indexing, sorting, exact matching</td>
      <td>Calculating mean, median, or sum</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td><code>Marks</code></td>
      <td>Integer (0–100)</td>
      <td>Quantitative / Ratio</td>
      <td>Sum, Mean (80.25), Std Dev, Min/Max</td>
      <td>Concatenation</td>
    </tr>
  </tbody>
</table>

**Verification and correction procedure:**
1. **Inspect the column:** `Roll_No` values are unique per student and follow an institutional code pattern (year + branch + serial number) — this is an identifier/categorical field, not a measurable quantity.
2. **Recognise meaningless aggregation:** 'Averaging' an identifier is mathematically meaningless — no real-world interpretation exists for an 'average roll number'.
3. **Re-prompt correctly:** *"Roll_No is a categorical identifier, not a quantity — do not compute statistics on it. Instead, compute the average of the Marks column."*
4. **Confirm the corrected computation:** Average Marks = (78 + 85 + 91 + 67) / 4 = 321 / 4 = **80.25**.

</details>

__*Key Concepts Applied*__

- This directly illustrates Section 3.5's data-type misinterpretation pitfall — a numeric-looking code is not numeric data in the mathematical sense.
- The fix is a re-prompt that explicitly states the correct data type/role of the column, which reliably steers the tool to the right computation.

<details>

<summary><b>Expected Output</b></summary>

A brief written note flagging that Roll_No must never be aggregated, plus the correct average of Marks (80.25), demonstrating the student can catch and correct this class of AI error independently.

</details>

<br/>
<PromptSandbox initialPrompt="I have a student dataset with columns: Student, Roll_No, Marks. Roll_No values are like 21A1001, 21A1002 etc. If I ask 'what is the average Roll_No?', what would you do and why?" />

<br/>
<CodeReference code={`# Demonstrate correct vs incorrect column handling
students = {
    "S1": {"Roll_No": "21A1001", "Marks": 78},
    "S2": {"Roll_No": "21A1002", "Marks": 85},
    "S3": {"Roll_No": "21A1003", "Marks": 91},
    "S4": {"Roll_No": "21A1004", "Marks": 67},
}

marks = [s["Marks"] for s in students.values()]
avg_marks = sum(marks) / len(marks)

print("Roll_No is a categorical identifier — averaging it is meaningless!")
print(f"Correct: Average Marks = {avg_marks:.2f}")`} />
<PyodideRunner initialCode={`# Type the code below this line to practice\n`} />

### <a id="_Toc239677991"></a>Experiment 3: Clean and Summarise a Small Sample Dataset

Problem: Use an AI tool to clean and summarise a small sample dataset (e.g., class attendance or marks data).

__*Step-by-Step Solution*__

**Raw Sample Dataset (Messy Records, Marks out of 100):**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Raw Student Entry</th>
      <th>Raw Marks Entry</th>
      <th>Data Quality Anomaly</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><code>Anil</code></td>
      <td><code>88</code></td>
      <td>Valid record</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><code>sunita</code></td>
      <td><code>  92</code></td>
      <td>Lowercase name; leading whitespace in numeric value</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td><code>RAVI</code></td>
      <td><code>N/A</code></td>
      <td>All uppercase name; missing numeric score</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>4</td>
      <td><code>Divya</code></td>
      <td><code>76</code></td>
      <td>Valid record</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>5</td>
      <td><code>kiran </code></td>
      <td><code>104</code></td>
      <td>Lowercase with trailing space; score out of valid range (> 100)</td>
    </tr>
  </tbody>
</table>

**Data Cleaning Pipeline & Instructions:**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Quality Issue Detected</th>
      <th>Affected Entry</th>
      <th>Cleaning Action Taken</th>
      <th>Methodological Rationale</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><strong>Inconsistent Casing</strong></td>
      <td><code>sunita</code>, <code>RAVI</code>, <code>kiran </code></td>
      <td>Convert to Title Case (<code>Sunita</code>, <code>Ravi</code>, <code>Kiran</code>)</td>
      <td>Cosmetic uniformity for formal reporting.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><strong>Stray Whitespace</strong></td>
      <td><code>'  92'</code>, <code>'kiran '</code></td>
      <td>Trim leading/trailing whitespace (<code>92</code>, <code>'Kiran'</code>)</td>
      <td>Prevents digits being treated as string text.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td><strong>Missing Value</strong></td>
      <td>Ravi: <code>'N/A'</code></td>
      <td>Exclude Ravi from numeric calculations</td>
      <td>Setting to 0 falsely pulls mean down from 85.3 to 64.0.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>4</td>
      <td><strong>Out-of-Range Value</strong></td>
      <td>Kiran: <code>104</code></td>
      <td>Flag as data-entry error; hold for human check</td>
      <td>Scores cannot exceed 100; silent auto-fixing corrupts truth.</td>
    </tr>
  </tbody>
</table>

**Correct Summary After Cleaning:**
- Valid entries evaluated: Anil (88), Sunita (92), Divya (76)
- Mean of valid entries: (88 + 92 + 76) / 3 = 256 / 3 ≈ **85.3**

__*Explanation of Approach*__

The exercise operationalises Section 3.5: an AI tool might silently 'fix' the missing or invalid values without telling the student, which would corrupt the summary. The correct workflow always surfaces data-quality issues for human decision rather than resolving them invisibly.

<br/>
<PromptSandbox initialPrompt="Clean and summarise this dataset. Standardise name casing, remove extra whitespace, flag missing values (N/A) rather than replacing with zero, and flag any impossible values. Report the average marks for valid entries only.

Student | Marks
Anil    | 88
sunita  | 92
RAVI    | N/A
Divya   | 76
kiran   | 104" />

<br/>
<CodeReference code={`# Reproduce the data cleaning steps manually in Python
raw = [
    ("Anil",  88),
    ("sunita", 92),
    ("RAVI",   None),   # N/A
    ("Divya",  76),
    ("kiran ", 104),    # trailing space + impossible value
]

print("Cleaned dataset:")
valid_marks = []
for name, marks in raw:
    clean_name = name.strip().title()
    if marks is None:
        print(f"  {clean_name}: MISSING — excluded from average")
    elif marks > 100:
        print(f"  {clean_name}: {marks} — FLAGGED as impossible, needs verification")
    else:
        print(f"  {clean_name}: {marks}")
        valid_marks.append(marks)

print(f"\nAverage (valid entries only): {sum(valid_marks)/len(valid_marks):.1f}")`} />
<PyodideRunner initialCode={`# Type the code below this line to practice\n`} />

### <a id="_Toc239677992"></a>Experiment 4: Three Different Chart Types from the Same Dataset

Problem: Generate three different chart types from the same dataset using AI prompts and justify which is most appropriate.

__*Step-by-Step Solution*__

Using the attendance dataset from Practical Example 1 (10 students across Sections A and B):


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Chart Requested</th>
      <th>Prompt Given to AI</th>
      <th>Appropriateness Evaluation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><strong>Bar chart</strong></td>
      <td><code>"Plot average attendance by section as a bar chart."</code></td>
      <td><strong>Most appropriate</strong> — compares a numeric average across 2 discrete categories clearly (Section 3.3).</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>2</td>
      <td><strong>Pie chart</strong></td>
      <td><code>"Plot a pie chart of how many students are above vs below 75% attendance."</code></td>
      <td><strong>Acceptable</strong> — only 2 binary categories (above/below threshold), suitable for showing proportion of the whole.</td>
    </tr>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>3</td>
      <td><strong>Scatter plot</strong></td>
      <td><code>"Plot a scatter of each student's attendance % against their roll index number."</code></td>
      <td><strong>Least appropriate</strong> — student roll index has no continuous numeric relationship with attendance; produces meaningless spread.</td>
    </tr>
  </tbody>
</table>

__*Explanation of Approach*__

The exercise deliberately includes one poorly-suited chart (scatter of attendance vs arbitrary roll index) so the student practices the judgement of Section 3.3 — being able to generate a chart is not the same as it being a meaningful one; the final justification should explicitly recommend the bar chart as best for this attendance-by-section comparison.

<br/>
<PromptSandbox title="Prompt for Bar Chart" initialPrompt="Plot average attendance by section as a bar chart. Data: Section A average = 83.6%, Section B average = 75.4%" />
<PromptSandbox title="Prompt for Pie Chart" initialPrompt="Plot a pie chart showing how many students are above vs below 75% attendance. Data: 6 students above 75%, 4 students below 75%" />
<PromptSandbox title="Prompt for Scatter Plot" initialPrompt="Plot a scatter of each student's attendance % against their roll index number (1-10). Data: S1=92,S2=78,S3=65,S4=88,S5=95,S6=70,S7=82,S8=60,S9=91,S10=74" />

<br/>
<CodeReference code={`import sys
sys.stdout.write("Note: matplotlib requires a display environment. In this browser runner, we demonstrate the data and logic.\n\n")

data = {"S1":92,"S2":78,"S3":65,"S4":88,"S5":95,"S6":70,"S7":82,"S8":60,"S9":91,"S10":74}
sec_a = [92, 78, 65, 88, 95]
sec_b = [70, 82, 60, 91, 74]

print("=== Bar Chart Data ===")
print(f"Section A avg: {sum(sec_a)/len(sec_a):.1f}%  (use a taller bar)")
print(f"Section B avg: {sum(sec_b)/len(sec_b):.1f}%  (use a shorter bar)")

print("\n=== Pie Chart Data ===")
above = sum(1 for v in data.values() if v >= 75)
below = len(data) - above
print(f"Above 75%: {above} students ({above/len(data)*100:.0f}%)")
print(f"Below 75%: {below} students ({below/len(data)*100:.0f}%)")

print("\n=== Scatter Data ===")
for i, (s, v) in enumerate(data.items(), 1):
    print(f"  Roll index {i} ({s}): {v}%")
print("\nJudgement: Bar chart is MOST appropriate — compares averages across 2 categories.")
print("Scatter plot is LEAST appropriate — roll index has no real relationship with attendance.")`} />
<PyodideRunner initialCode={`# Type the code below this line to practice\n`} />

### <a id="_Toc239677993"></a>Experiment 5: Cross\-Check One AI\-Generated Statistical Claim Manually

Problem: Cross-check one AI-generated statistical claim manually and report any discrepancy found.

__*Step-by-Step Solution*__

Suppose an AI tool, given the Section B attendance data (70, 82, 60, 91, 74), reports: "The average attendance in Section B is 76.4%."

**Manual Verification Cross-Check:**
- Sum: $70 + 82 + 60 + 91 + 74 = 377$
- Count ($N$): $5$
- Correct Mean: 377 / 5 = **75.4%**

**Statistical Discrepancy Analysis:**


<table>
  <thead>
    <tr>
      <th style={{ textAlign: "center", width: "80px" }}>Sl. No.</th>
      <th>Evaluation Parameter</th>
      <th>AI-Reported Claim</th>
      <th>Manually Verified Value</th>
      <th>Observed Discrepancy & Root Cause</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ textAlign: "center", fontWeight: "bold" }}>1</td>
      <td><strong>Section B Mean Attendance</strong></td>
      <td><code>76.4%</code></td>
      <td><code>75.4%</code> (377 / 5 = 75.4%)</td>
      <td><strong>Discrepancy of 1.0 percentage point</strong>: caused by an arithmetic summation slip by the LLM.</td>
    </tr>
  </tbody>
</table>

__*Explanation of Approach*__

This exercise is deliberately designed to surface a discrepancy so students practice the full verification workflow: recompute independently using basic arithmetic, compare exactly, and report the size and likely cause of any mismatch, rather than assuming the AI's number is correct because it was stated confidently and formatted like a fact.

__*Alternative Method*__

A student could cross-check using a spreadsheet's built-in AVERAGE() function as a second independent tool, which would also return 75.4%, corroborating the manual calculation and further confirming the AI's figure was in error.

<br/>
<PromptSandbox initialPrompt="Given this dataset for Section B attendance: 70, 82, 60, 91, 74 — what is the average attendance percentage?" />

<br/>
<CodeReference code={`# Cross-check the AI's claimed value of 76.4% against the correct calculation
section_b = [70, 82, 60, 91, 74]

correct_mean = sum(section_b) / len(section_b)
ai_claimed = 76.4

print(f"Values: {section_b}")
print(f"Sum = {sum(section_b)}, Count = {len(section_b)}")
print(f"Correct Mean = {correct_mean:.1f}%")
print(f"AI Claimed   = {ai_claimed}%")
print(f"Discrepancy  = {abs(ai_claimed - correct_mean):.1f} percentage point")
print()
if abs(ai_claimed - correct_mean) > 0.05:
    print("DISCREPANCY FOUND — the AI's figure is incorrect!")
else:
    print("Values match — AI result verified.")`} />
<PyodideRunner initialCode={`# Type the code below this line to practice\n`} />
